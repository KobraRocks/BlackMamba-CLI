export const basicCmds = new Set([
    '-v', '--version', 'version',
    '-h', '--help', 'help'
]);

export const advancedCmds = new Set([
    '-c', '--create', 'create',
    '-r', '--remove', 'remove'
]);


export const features = new Set([
    'project',
    'source',
    'package',
]);
